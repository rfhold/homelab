#!/usr/bin/env bash
set -euo pipefail

output=
while (($#)); do
  case "$1" in
    --output)
      output=$2
      shift 2
      ;;
    *)
      shift
      ;;
  esac
done

payload=$(cat)
printf '%s' "$payload" >"$FIXTURE_STATE/age-input"
[[ "${MOCK_AGE_FAIL:-}" != 1 ]] || exit 1
printf 'age-encrypted-fixture' >"$output"
