apiVersion: rules.alerting.grafana.app/v0alpha1
kind: AlertRule
metadata:
  annotations:
    grafana.app/folder: alert-rules-node
    grafana.com/provenance: converted_prometheus
  labels:
    grafana.app/folder: alert-rules-node
  name: ar-fdd8a8b517305fe6f9b086cc
spec:
  annotations:
    description: Filesystem on {{ $labels.device }}, mounted on {{
      $labels.mountpoint }}, at {{ $labels.instance }} has only {{ printf "%.2f"
      $value }}% available space left.
    summary: Filesystem has less than 3% space left.
  execErrState: Error
  expressions:
    A:
      datasourceUID: mimir
      model:
        datasource:
          type: prometheus
          uid: mimir
        expr: >-
          (
            node_filesystem_avail_bytes{job=~"integrations/(node_exporter|unix)",fstype!="",mountpoint!=""} / node_filesystem_size_bytes{job=~"integrations/(node_exporter|unix)",fstype!="",mountpoint!=""} * 100 < 3
          and
            node_filesystem_readonly{job=~"integrations/(node_exporter|unix)",fstype!="",mountpoint!=""} == 0
          )
        instant: false
        intervalMs: 1000
        maxDataPoints: 43200
        range: true
        refId: A
      relativeTimeRange:
        from: 10m0s
        to: 0s
    B:
      model:
        datasource:
          type: __expr__
          uid: __expr__
        expression: A
        intervalMs: 1000
        maxDataPoints: 43200
        refId: B
        reducer: last
        settings:
          mode: dropNN
        type: reduce
      source: true
  for: 30m0s
  labels:
    severity: critical
  noDataState: OK
  title: NodeFilesystemAlmostOutOfSpace
  trigger:
    interval: 1m
