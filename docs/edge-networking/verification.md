# Edge Verification

Pantheon live verification on 2026-08-20 confirmed the Agent Gateway migration and Admin UI route described below. Other cluster, DNS, certificate, network, and Cloudflare topics remain unresolved unless the table records dated evidence.

| Topic | Tracked evidence | Unresolved verification |
| --- | --- | --- |
| kgateway and default Gateways | Chart versions, GatewayClass, listeners, and certificates are declared in source. On 2026-08-20, Pantheon Gateway API CRDs reported bundle `v1.6.0` and the experimental channel, the safe-upgrades admission policy and binding existed, and the ingress stack reported 187 unchanged resources. | Confirm the kgateway controller version, default Gateway listener status, and LoadBalancer addresses in Pantheon; verify the full contract separately in Romulus. |
| Agent Gateway and Codex routes | Agent Gateway model and Admin UI routes, the Admin UI Service, the admin listener parameters, and the Codex administrative route are represented in source. Tracked source routes exact `/config_dump` to the Admin UI Service and excludes it from body-derived model extraction. On 2026-08-20, both Agent Gateway routes were accepted with resolved references, both policies were accepted and attached at current generations, the Service had a ready port `15000` endpoint, `/` returned the `/ui` redirect, and `/ui/` and `/api/config` returned `200`. On 2026-08-21, the Admin UI route and model policy update completed, both resources reported accepted current generations, public `/config_dump` and `/ui/` returned `200`, each HTTP and HTTPS listener exposed nine `agent-gateway` inference route rules plus one admin route in the xDS dump, and the Agent Gateway stack reported 39 unchanged resources. | Confirm successful model inference, requests above the default 2 MiB body buffer, sustained health, and the absence of a LiteLLM compatibility hostname. Admin UI authentication remains undefined by the intended route contract. |
| Technitium clustering | Source declares Romulus primary, Pantheon secondary, catalog membership, and TSIG-authorized transfer options | Confirm both nodes report the expected cluster relationship, the secondary catalog is current, SOA serials converge, and both servers answer authoritatively. |
| ExternalDNS | Ingress source points RFC 2136 updates at the primary and watches Gateway API routes | Confirm expected A/TXT records exist, ownership records are not conflicting, and updates are accepted with the configured TSIG key. |
| Certificates | cert-manager resources request wildcard coverage including `*.rholden.dev` | Confirm issued certificate readiness, Secret freshness, and the certificate served for each local alias. |
| Tunnel parity | Source retains the two Romulus Cloudflare Tunnel routes while Pantheon adds local aliases | Confirm public DNS and tunnel origins were not changed outside tracked source, and verify local and public resolution from their intended client networks. |

Use [Technitium secondary recovery](operations/technitium-secondary-recovery.md) only after read-only checks identify the secondary or catalog as the failing boundary.
