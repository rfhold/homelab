# Secrets Verification

Dated Kubernetes inspections, Pulumi operations, authenticated API reads, public OIDC metadata, and user-confirmed login established the observations below. Repository source describes intended and tracked behavior; it is not live-state evidence.

| Topic | Tracked or observed evidence | Unresolved verification |
| --- | --- | --- |
| Pantheon deployment | An authorized 2026-08-09 apply and readiness-wait recovery created the tracked stack without replacement or deletion. After bootstrap, live inspection found three Ready pods with zero restarts on distinct nodes: ordinal zero on `artemis`, ordinal one on `athena`, and ordinal two on `apollo`. Three distinct retained `10Gi` RWO `database` PVCs were Bound. All five Services, PDB `maxUnavailable: 1`, and the HTTPRoute Accepted with resolved references on `ingress/default-gateway#https-0`. Canonical DNS resolved to Pantheon `172.16.3.53`. | Continue routine health observation. Deployment evidence does not establish backup or disaster-recovery readiness. |
| Legacy Romulus | Live inspection on 2026-08-09 found `statefulset/openbao-chart` scaled to `0`, no pod or HTTPRoute, and one bound `10Gi` `shared-fs` PVC. After the Pantheon-only decision, the authorized 2026-08-13 `openbao/romulus` destroy deleted exactly 12 resources in 19 seconds: one ConfigMap, one ServiceAccount, one ClusterRoleBinding, three Services, one StatefulSet, one Helm Chart, one component, one module, one namespace, and one stack resource. Read-only verification then found zero managed resources in the retained empty Pulumi stack record, namespace `openbao` absent on Romulus, the binding absent, and no matching StatefulSet, Service, or PVC across namespaces. | The Pulumi stack history and configuration record still exists but manages no resources. Removing it with `pulumi stack rm romulus` requires separate explicit authorization. |
| Initialization and unseal | The user completed 5-of-3 Shamir initialization and custody outside recorded transcripts. Sanitized status from every voter reported `initialized: true`, `sealed: false`, and HA enabled. No share or token value was inspected or recorded. | Manual unseal remains required after a sealed restart. Recovery-material availability and independent custody were user-managed and were not independently inspected. |
| Raft membership and failover | Live status and bounded logs showed ordinal zero active, ordinals one and two standby, all three promoted to voters, and each at committed and applied Raft index 33. The active-only UI EndpointSlice selected only ordinal zero, and the PDB reported three healthy voters with one disruption allowed. Canonical `/v1/sys/health` returned HTTP 200 before failover. | Controlled leader step-down and post-failover canonical service verification remain separately gated. |
| Transit | After authorized inventory found no planned objects, a reviewed 2026-08-09 preview and apply created Transit mount `transit`, key `pulumi`, policy `pulumi-transit`, and a `cyber` OIDC role limited to Transit encrypt and decrypt for key `pulumi`. Sanitized reads confirmed AES256-GCM96 key version 1, non-exportable and non-deletable key settings, and the exact Transit policy boundary. The authorized 2026-08-13 Pantheon cleanup apply matched preview: two component-input updates, five obsolete snapshot-auth deletions, 30 unchanged, and no replacement. It deleted the snapshot Kubernetes role, Kubernetes auth configuration, snapshot policy, Kubernetes auth backend, and TokenReview ClusterRoleBinding while Transit mount/key, `pulumi-transit`, `cyber`, OIDC, route, storage, and three Running Pantheon pods remained. | Transit remains intended and live. The cleanup provides no snapshot, backup, restoration, or DR readiness evidence. |
| Authentik OIDC | Source assigns `AuthentikOIDCApp("openbao-oidc")`, its random client secret, provider, application, policy binding, and dedicated RSA-4096 signing key pair to `openbao/pantheon`; registration is Pantheon-only and otherwise disabled. It looks up the existing group named exactly `cyber`, uses strict UI and CLI redirects, a confidential client, and policy engine `all`. On 2026-08-09, protected checkpoints and the exact four-resource ownership move completed. The authorized destination preview and apply then updated the Application in place to policy engine `all`, created one binding for the resolved group at order zero, and added output changes without Kubernetes operations, replacement, deletion, or RandomPassword operation. After an initial `cyber` member UI login failed because Authentik issued HS256 while OpenBao required RS256, authorized certificate inventory found no suitable OpenBao-specific pair. A separately reviewed preview and apply created the dedicated RSA key, certificate, and Authentik pair and updated existing OAuth2 provider ID `12` in place; 24 resources were unchanged, private key material remained masked, and no application replacement, client-secret rotation, OpenBao API change, or unrelated operation occurred. Public discovery then advertised only RS256, and sanitized JWKS metadata reported one RSA signing key with `alg: RS256` and `use: sig`. The user subsequently confirmed successful authenticated OpenBao UI and CLI login for a `cyber` member. | The issued member token's policies were not inspected, and non-member denial remains unverified. The user declined the proposed checkpoint-only cleanup, so `authentik/romulus` retains six obsolete masked OIDC output values; never apply or target that stack for stale output cleanup. |
| OpenBao OIDC API | Source pins `@pulumi/vault@7.11.0`, enables API management only in `openbao/pantheon`, requires a non-empty runtime `VAULT_TOKEN`, and uses an explicit canonical-address `vault.Provider` with no token argument. Native `vault.jwt.AuthBackend` and `vault.jwt.AuthBackendRole` resources derive configuration from the stack-owned Authentik component. The client secret uses an explicitly versioned write-only input. After authorized inventory confirmed both objects absent, the reviewed preview and apply created only the explicit provider, OIDC backend, and role, with 23 resources unchanged and no replacement, deletion, secret rotation, Kubernetes operation, or Authentik operation. Sanitized API reads confirmed a non-local `oidc` mount and role `operator` with type OIDC, claim `sub`, both exact callbacks, `token_no_default_policy: true`, and only `default`; discovery returned the expected issuer and endpoints. A `cyber` member completed UI and CLI login after the dedicated RS256 signing key was reconciled. | Provider compatibility, declarative API reconciliation, and member UI and CLI authentication are established. The issued token's policy set and non-member denial remain unverified. |
| Pulumi and Tekton | Current Tekton source still uses `PULUMI_CONFIG_PASSPHRASE`. Only `openbao-secrets-canary/canary` declares `hashivault://pulumi`; no workload stack changed providers. | Keep Tekton and other stacks on their current providers. Any broader migration requires a separate contract and approval. |
| Canary provider | On 2026-08-09, stack `openbao-secrets-canary/canary` was initialized directly with `hashivault://pulumi`. A generated harmless secret remained masked. The authorized update created only one Pulumi stack resource, and an immediate preview reported that resource unchanged with no provider-managed infrastructure. | This establishes the Pantheon Transit provider path only. It is not restoration or DR evidence. |
| Backup and DR target | The approved target has no OpenBao snapshots, backup workload, object-storage contract, age ceremony, restore procedure, Romulus DR deployment, recovery route, or backup/DR alert contract. Raft and Ceph HA are not backups. The obsolete snapshot API resources and legacy Romulus deployment were removed on 2026-08-13 under separate authorization. | No backup or DR readiness claim exists. Transit and the HashiVault canary remain, but neither is backup, restoration, failover, or DR evidence. |
| Historical backup artifact | A multi-architecture backup image was published on 2026-08-09, and two historical commits remain. | Historical retention does not create an active source, support, publication, or maintenance contract. |
| Legacy retirement | Romulus was retained during the Pantheon rollout, then intentionally destroyed on 2026-08-13 after the Pantheon-only decision. The exact 12-resource destroy and subsequent namespace, binding, workload, Service, and PVC absence checks are recorded above. | Only the empty Pulumi stack history and configuration record remains. Its separately authorized removal must not be conflated with managed-resource destruction. |
| Transport TLS | The live HTTPRoute was Accepted on `ingress/default-gateway#https-0`, and canonical `/v1/sys/health` returned HTTP 200 over HTTPS. Its backend API connection remains HTTP, while the chart advertises OpenBao's server-to-server cluster channel as HTTPS for built-in mutual TLS. | Implement operator-managed end-to-end API certificates before treating backend client transport as complete. |
| Workload source of truth | Pulumi config and Kubernetes Secrets remain the tracked delivery path. | Confirm future values individually as they move into OpenBao; no workload migration is part of this rollout. |

Pantheon OpenBao is an established live HA service with reconciled OIDC and Transit resources, a working HashiVault canary, and successful Authentik-backed UI and CLI login for a tested `cyber` member. The obsolete snapshot-auth resources and legacy Romulus deployment were removed on 2026-08-13. Controlled failover, issued-token policy inspection, and non-member denial remain unverified. There is no OpenBao backup or DR deployment or readiness claim, and the empty Romulus Pulumi stack history and configuration record remains separately gated for removal.
