# Runtime Verification

These items remain unresolved. Tracked source is evidence of repository implementation, not proof of rendered output or live cluster state.

| Topic | Source evidence | Unresolved verification |
| --- | --- | --- |
| Legacy `ai-inference` retirement | Historical completion evidence dated 2026-05-24 at revision `316959090d82d223693858ad8690f4d6f1561f4c` records a successful Pantheon stack destruction with 11 resources deleted and a subsequent `NotFound` response for the namespace. Current source contains no `programs/ai-inference` program. | This is point-in-time evidence, not proof of current cluster state. No live inspection was performed during this conversion, so current namespace and resource absence remains unverified. |
| Athena GPU sharing boundary | [`Pulumi.qwen3-embedding.yaml`](../../programs/vllm/Pulumi.qwen3-embedding.yaml) intentionally selects the NVIDIA runtime and Athena while requesting only CPU and memory, preserving scheduler sharing with Gemma and planned speech services | No tracked resource accounting or formal GPU partition divides Athena capacity among these workloads. Scheduling compatibility, device access, memory contention, and safe concurrent operation remain unverified. |
| Mars Qwen3.8 migration, fit, and behavior | Current source configures the active [`llama-cpp/Pulumi.qwen3.8-27b.yaml`](../../programs/llama-cpp/Pulumi.qwen3.8-27b.yaml) Q8_K_P runtime with automatic BF16 projector discovery, embedded MTP depth `2`, `262144` context, one slot, and one RTX A6000 GPU; the prior [`vllm/Pulumi.qwen3.8-27b.yaml`](../../programs/vllm/Pulumi.qwen3.8-27b.yaml) FP8 runtime remains at zero replicas. The following evidence is historical and applies only to the superseded FP8/vLLM runtime, not to the GGUF/llama.cpp desired state. On 2026-08-24, the Pantheon rollout without forced eager execution became ready with zero restarts. vLLM reported full and piecewise CUDA graph capture using 0.46 GiB, 12.27 GiB available KV cache, successful multimodal warmup, and a 385,683-token GPU KV cache. Three sequential 64-prompt-token, 256-completion-token requests completed in 13.936, 10.914, and 10.912 seconds; the first request triggered three runtime Triton JIT compilations, while the latter two each sustained 23.46 output tokens per second versus a 7.14-token-per-second eager baseline. A same-day one-token MTP rollout loaded the model's `Qwen3_5MTP` draft head, shared its embedding and output-head weights, completed multimodal warmup, and became ready with zero restarts. FlashInfer limited speculative decoding to piecewise CUDA graphs. vLLM reported 12.17 GiB available KV cache and a 347,561-token GPU KV cache. Three matched requests completed in 8.174, 7.787, and 7.880 seconds at 31.32, 32.88, and 32.49 output tokens per second. Median one-token MTP throughput improved 38.5% over the CUDA-graph-only result and 4.55 times over eager execution. Draft acceptance reached approximately 71-73% during those requests. A same-day three-token MTP rollout became ready with zero restarts and completed multimodal warmup. vLLM reported piecewise CUDA graphs, 12.15 GiB available KV cache, a 326,285-token GPU KV cache, and 2.49-times maximum concurrency at the configured context length. Three matched requests completed in 6.910, 6.531, and 6.514 seconds at 37.05, 39.20, and 39.30 output tokens per second. Median three-token MTP throughput improved 20.7% over one-token MTP, 67.1% over CUDA graphs alone, and 5.49 times over eager execution. Draft acceptance varied by position and reached approximately 49-53% in the final measured windows. | The historical vLLM results do not establish comparative performance or capacity for llama.cpp. Point-in-time GGUF rollout evidence and its remaining verification gaps are recorded in the following row. |
| Mars Qwen3.8 Q8_K_P rollout | On 2026-08-27, the Pantheon vLLM stack was scaled to zero before the llama.cpp stack was created because both request Mars's NVIDIA GPU. The first llama.cpp create exposed a cluster-wide PersistentVolume name collision with the rollback stack; the active resources were qualified with `-llama-cpp`, after which the Q8_K_P model and BF16 projector loaded with zero pod restarts. Direct health and model-list requests succeeded, and direct text inference returned the requested sentinel while reporting embedded-MTP draft acceptance. Agent Gateway was then changed from `vllm-local-small` to `llama-cpp-local-small`. Public `local-small` text, required tool-call, and one-pixel PNG multimodal requests all succeeded and returned model identity `qwen3.8-27b`; the text and tool requests reported accepted MTP draft tokens. | This is point-in-time smoke evidence, not a sustained benchmark. Long-context behavior, concurrent requests, larger and varied images, multi-turn tool execution, quality, load behavior, memory headroom, throughput distribution, failure recovery, and sustained health remain unverified. The cold create exceeded Pulumi's prior default ten-minute readiness timeout before the pod became ready; source now assigns llama.cpp Deployments two-hour Pulumi create and update timeouts, but a subsequent cold create has not independently verified that timeout change. Public `/v1/models` returned `404`; alias functionality was verified through `/v1/chat/completions`. |
| Mars Qwen3.8 262K context | On 2026-08-27, the first `262144`-context rollout with default F16 K and V caches became ready with only 62 MiB of reported GPU memory free, logged a failed 248.10 MiB CUDA allocation for the multimodal path, and exited with code `139` on the first smoke requests. The corrected rollout used `q8_0` K and V caches, became ready in 68 seconds with zero restarts and approximately 6.45 GiB free, loaded the BF16 projector without allocation errors, and advertised both runtime and trained context as `262144`. Public `local-small` text and vision requests succeeded with embedded MTP active. A 200,021-token prompt returned the requested sentinel with `truncated = 0`; extending the same prefix reused 199,547 tokens, evaluated 55,516 more, returned the near-limit sentinel, and also reported `truncated = 0`. Final llama.cpp, vLLM, and Agent Gateway previews reported no changes after the rollout. | This later evidence resolves the preceding rollout row's long-context and memory-headroom gaps for one slot and the tested request shapes. Q8_0-cache quality versus F16, unrelated prompt distributions, larger images, sustained mixed workloads, recovery, and long-duration health remain unverified. |
| Qwen3.6 rollback readiness | [`Pulumi.qwen3.6-35b-a3b.yaml`](../../programs/llama-cpp/Pulumi.qwen3.6-35b-a3b.yaml) retains the llama.cpp resources, extended startup probe, model source, storage, and placement at zero replicas; Agent Gateway does not advertise the old alias | Zero replicas preserves tracked rollback intent but does not prove that the model cache is populated, the image can start, the GPU is available, or a future explicitly authorized scale-up and gateway cutback would succeed. |
| Universal workload labels | [`src/types.ts`](../../src/types.ts) provides generic propagation and many stacks supply labels | Standalone [`vllm`](../../programs/vllm/index.ts) and [`llama-cpp`](../../programs/llama-cpp/index.ts) do not consume the label helper, and the test suite proves representative labels rather than every workload-deploying stack. Full stack coverage remains incomplete. |
| Zot approval and rollout | [`zot-registry.ts`](../../src/components/zot-registry.ts) contains the startup probe and disabled UI/search; the stack requests managed TLS | The pre-conversion Zot lifecycle record was a draft with no approval and no completed stage. Its claims about cache size, restart pressure, certificate expiry, and deployment are not current evidence. Determine whether the tracked behavior should be formally approved, and verify any live rollout separately. |
| Gemma vLLM experiment | [`Pulumi.gemma-4-e2b.yaml`](../../programs/vllm/Pulumi.gemma-4-e2b.yaml) remains tracked and historical preview evidence showed renderability | Later decision evidence records that vLLM could not map the model's GGUF vision-tower parameters. Agent Gateway no longer advertises Gemma. Whether the experimental vLLM stack or standalone llama.cpp stack exists live or should be removed is unresolved; neither is an active serving claim. |
| Runtime deployment and health | Program and component source defines desired Kubernetes resources, including exact `/config_dump` Admin UI routing. Pantheon configuration also excludes exact `/v1/audio/transcriptions` and `/v1/audio/speech` from body-derived model extraction. On 2026-08-20, the Pantheon Agent Gateway controller and proxy Deployments were fully observed and ready on `v1.4.1`; the proxy pod had zero restarts, its Admin UI Service had a ready endpoint, and public `/ui/` and `/api/config` requests returned `200`. On 2026-08-21, the Admin UI route and model policy update completed, public `/config_dump` and `/ui/` returned `200`, each HTTP and HTTPS listener exposed nine `agent-gateway` inference route rules plus one admin route in the xDS dump, and the Agent Gateway stack reported 39 unchanged resources. On 2026-08-27, public `local-small` text, required tool-call, and multimodal inference succeeded after the llama.cpp cutover; final credentialed previews reported the llama.cpp and vLLM stacks at eight unchanged resources each and Agent Gateway at 37 unchanged resources. | Verify the audio exclusions, workload-owned HTTPRoutes, direct hostnames, Service EndpointSlices, large requests, and sustained gateway health live. Public `/v1/models` continued to return `404`; model alias routing was verified through `/v1/chat/completions`. |
| Audio workload ownership | Current homelab source contains no audio HTTPRoute and no Kokoro workload stack. The model-gateway contract assigns ordinary Service-backed routes to `rfhold/whisperx-server` and `rfhold/kokoro-server`. | Verify each workload repository implements its exact shared-host path, direct hostname, Service backend, and public unauthenticated posture. Verify live route precedence and EndpointSlice readiness separately. |

The historical Gemma rationale is retained in [the serving decision](decisions/gemma-serving.md). The normative GPU-sharing and label expectations remain in the relevant specifications rather than being presented as verified runtime behavior.
