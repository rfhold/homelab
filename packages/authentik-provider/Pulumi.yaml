name: authentik-provider
runtime: nodejs
description: Shared Authentik provider SDK for homelab
packages:
  authentik:
    source: terraform-provider
    version: 1.0.2
    parameters:
      - goauthentik/authentik
